#include <vector>
#include <iostream>
#include <algorithm>

using namespace std;

int maxSubarraySumCircular(vector<int>& nums) {
    // maximize the number of positive elements
    // if no positive elements, return the max element
    // if all positive elements, return the sum of all elements
    
}